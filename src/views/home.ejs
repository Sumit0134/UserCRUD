<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon" />
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body class="flex flex-col min-h-screen bg-zinc-900 text-white">
    <div class="w-full min-h-screen text-white p-10 bg-zinc-900">
      <%- include("./partials/header"); %>

      <h1 class="text-3xl m-2">Create an Account</h1>
      <form action="/api/v1/createUser" method="post">
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="text"
          placeholder="Enter your name"
          name="name"
        />
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="text"
          placeholder="Enter your username"
          name="userName"
        />
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="email"
          placeholder="Enter your email"
          name="email"
        />
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="password"
          placeholder="Enter your password"
          name="password"
        />
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="number"
          placeholder="Enter your age"
          name="age"
        />
        <button
          class="rounded-md bg-blue-600 px-8 cursor-pointer py-2 m-2"
          type="submit"
        >
          Create
        </button>
      </form>

      <h1 class="text-3xl mt-10 mb-10 m-2">Or</h1>
      <h1 class="text-3xl m-2">Login your Account</h1>
      <form action="/api/v1/loginUser" method="post">
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="email"
          placeholder="Enter your email"
          name="email"
        />
        <input
          class="rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3 py-2 m-2"
          type="password"
          placeholder="Enter your password"
          name="password"
        />
        <button
          class="rounded-md bg-blue-600 px-8 cursor-pointer py-2 m-2"
          type="submit"
        >
          Login
        </button>
      </form>

      <h1 class="text-3xl mt-10 mb-3 m-2">Some Recent Posts of Users</h1>
      <% if (typeof recentPosts !== "undefined" && recentPosts.length > 0) { %>
        <% recentPosts.forEach(post => { %>
          <div class="flex flex-wrap gap-3">
            <div class="p-4 m-2 border border-zinc-800 rounded-md bg-zinc-800">
              <h2 class="text-xl font-semibold"><%= post.title %></h2>
              <p class="text-sm text-zinc-400">
                By <%= post.user ? post.user.name : "Unknown" %> • 
                <%= post.createdAt ? post.createdAt.toDateString() : "No Date" %>
              </p>
              <p class="mt-2"><%= post.content %></p>
            </div>
            <% }); %>
          </div>
      <% } else { %>
        <p class="text-zinc-400 m-2">No recent posts available.</p>
      <% } %>
      
    </div>
      <footer class="w-full bg-zinc-900 p-4 mt-10 text-center">
        <p class="text-light text-sm text-zinc-500">
          Copyright &copy; 2025 UserCRUD ~ All rights reserved
        </p>
      </footer>
  </body>
</html>
