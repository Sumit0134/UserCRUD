<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon" />
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <style>
      .logout-container {
        position: absolute;
        top: 1rem;
        right: 1rem;
      }

      @media (max-width: 768px) {
        .text {
          width: 100% !important; 
        }

        .logout-container {
          position: absolute;
          top: 1rem;
          right: 1rem;
        }

        textarea {
          width: 100% !important;
        }

        button[type="submit"] {
          width: 100% !important; 
        }

        .w-full {
          padding-left: 1rem;
          padding-right: 1rem;
        }

        h1,
        h5 {
          text-align: center;
        }

        .text-white {
          font-size: 16px; 
        }

        .post {
          width: 100% !important;
        }
      }

      @media (min-width: 768px) {
        .text {
          width: 33.33% !important; 
        }

        button[type="submit"] {
          width: auto; 
        }
      }
    </style>
  </head>
  <body>
    <div class="w-full min-h-screen text-white p-10 bg-zinc-900">
      
      <div class="logout-container">
        <a
          class="bg-red-600 px-3 py-2 rounded-md text-white cursor-pointer"
          href="/api/v1/logout"
          ><button>Logout</button></a
        >
      </div>
      <%- include("./partials/header"); %>
      
      <h5 class="mt-5 mb-3">Edit Your Post</h5>
      <form action="/api/v1/updatePost/<%= post._id %>" method="post">
        <textarea
          class="text w-1/3 bg-transparent border-2 border-zinc-800 resize-none rounded-md block outline-none px-3 py-2"
          placeholder="What's on your mind?"
          name="content"
        >
<%= post.content %></textarea
        >
        <button
          class="w-40 bg-yellow-600 text-black px-8 cursor-pointer block mt-3 py-2 rounded-md"
          type="submit"
        >
          Update Post
        </button>
      </form>

      <%- include("./partials/footer"); %>
    </div>
  </body>
</html>
